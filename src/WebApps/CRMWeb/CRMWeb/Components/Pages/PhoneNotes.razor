@page "/phone-notes"
@rendermode InteractiveServer
@attribute [StreamRendering]
@using CRMWeb.Models.Phones
@using CRMWeb.Services.PhoneNotes

<PageTitle>Phone notes</PageTitle>

<h3>Phone notes</h3>

<span class="error">@ErrorDetails</span>

<SfGrid TValue=PhoneNoteModel" @ref="PhoneNoteGrid"
        AllowPaging="true" AllowSorting="true" AllowFiltering="true" AllowGrouping="true"
        ShowColumnMenu="true" AllowResizing="true" AllowReordering="true"
        Toolbar="@toolbar">
    <GridEvents TValue="PhoneNoteModel" OnActionFailure="@HandleActionFailure" OnActionBegin="@HandleActionBegin"></GridEvents>
    <GridPageSettings PageSize="6"></GridPageSettings>
    <SfDataManager AdaptorInstance="@typeof(PhoneNoteAdaptor)" Adaptor="Adaptors.CustomAdaptor"></SfDataManager>
    @* <GridGroupSettings ShowGroupedColumn="true"></GridGroupSettings> *@
    <GridFilterSettings Type="Syncfusion.Blazor.Grids.FilterType.Menu"></GridFilterSettings>

    <GridEditSettings AllowAdding="true" AllowEditing="true" AllowDeleting="true" Mode="EditMode.Dialog" Dialog="DialogParams">
        <HeaderTemplate>
            @{
                var DialogHeader = GetHeader((context as PhoneNoteModel));
                <span>@DialogHeader</span>
            }
        </HeaderTemplate>

        <Template>
            @{
                var PhoneNote = (context as PhoneNoteModel);
                var IdValue = (PhoneNote!.Id).ToString() ?? string.Empty;

                <FluentValidator TValidator="PhoneNoteValidator" />
                <ValidationSummary />

                <span class="error">@ErrorDialodDetails</span>

                <div>
                    <div class="row">
                        <div class="form-group col-md-12">
                            <SfTextBox ID="Name"
                                       @bind-Value=@(PhoneNote.Name)
                                       FloatLabelType="FloatLabelType.Always"
                                       Placeholder="Name"></SfTextBox>
                        </div>
                    </div>
                </div>

                <div>
                    <div class="row">
                        <div class="form-group col-md-9">
                            <SfTextBox ID="Id"
                                       @bind-Value=@(IdValue)
                                       FloatLabelType="FloatLabelType.Always"
                                       Placeholder="Id"
                                       Enabled=false>
                            </SfTextBox>
                        </div>
                    </div>
                </div>
            }
        </Template>
    </GridEditSettings>

    <GridColumns>
        <GridColumn Field=@nameof(PhoneNoteModel.Name) HeaderText="Name"></GridColumn>
        <GridColumn Field=@nameof(PhoneNoteModel.Id) HeaderText="Id" IsPrimaryKey="true" ValidationRules="@(new ValidationRules { Required = true })" TextAlign="@TextAlign.Left" HeaderTextAlign="@TextAlign.Left" Width="200" Visible="false"></GridColumn>
    </GridColumns>
</SfGrid>
@* 
<AuthorizeView Policy="ProductWrite">
    <button @onclick="CreateProduct">Add Product</button>
</AuthorizeView> *@

<style>
    .error {
        color: red;
    }
</style>

@code {
    private string ErrorDetails = string.Empty;
    private string ErrorDialodDetails = string.Empty;

    private SfGrid<PhoneNoteModel> PhoneNoteGrid { get; set; } = new();
    private DialogSettings DialogParams = new DialogSettings { MinHeight = "400px", Width = "400px" };

    private List<object> toolbar = new List<object> { "Add", "Edit", "Delete", "Search" };

    private List<PhoneNoteModel> PhoneNoteData { get; set; } = [];

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }

    public void HandleActionFailure(Syncfusion.Blazor.Grids.FailureEventArgs args)
    {
        bool dialog = false;
        var errordetails = args.Error.Message;

        try
        {
            var apiException = args.Error.InnerException as Refit.ApiException;
            if (apiException is not null
                && (apiException.HttpMethod.Method == "POST"
                    || apiException.HttpMethod.Method == "PUT"
                ))
            {
                dialog = true;
            }

            if (errordetails.Contains("ValidationErrors"))
            {
                var msgs = JsonDocument.Parse(errordetails).RootElement
                    .GetProperty("ValidationErrors")
                    .EnumerateArray()
                    .Select(e => e.GetProperty("errorMessage").GetString())
                    .Where(s => !string.IsNullOrWhiteSpace(s));

                if (dialog)
                    ErrorDialodDetails = string.Join(" | ", msgs);
                else
                    ErrorDetails = string.Join(" | ", msgs);
            }
            else if (errordetails.Contains("detail"))
            {
                var detail = JsonDocument.Parse(errordetails).RootElement.GetProperty("detail").GetString();
                if (dialog)
                    ErrorDialodDetails = detail!;
                else
                    ErrorDetails = detail!;
            }
            else
            {
                if (dialog)
                    ErrorDialodDetails = errordetails;
                else
                    ErrorDetails = errordetails;
            }
        }
        catch
        {
            if (dialog)
                ErrorDialodDetails = errordetails;
            else
                ErrorDetails = errordetails;
        }

        StateHasChanged();
    }

    public void HandleActionBegin(ActionEventArgs<PhoneNoteModel> args)
    {
        ErrorDetails = string.Empty;
        ErrorDialodDetails = string.Empty;
    }

    // private bool IsValid(PhoneNoteModel data)
    // {
    //     return !string.IsNullOrWhiteSpace(data.Name) && !string.IsNullOrWhiteSpace(data.ImageFile);
    // }

    public string GetHeader(PhoneNoteModel? value)
    {
        return value?.Id == Guid.Empty
            ? "New unit of measurement"
            : $"Edit: '{value?.Name}'";
    }
}


